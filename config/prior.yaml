# @package _global_

defaults:
  - data: metaworld
  - policy: skill_gpt
  - train: default
  - _self_

# change defaults
data:
  seq_len: 16
  obs_seq_len: 1
train:
  batch_size: 256
  num_workers: 4
  n_epochs: 50
  save_interval: 5
tune_decoder: false

pretrain_model_path: ""

exp_name: "v1td_16_f4_k3s2_tt_n6d384" # 
seed: 10000

use_wandb: true
wandb_project: "skill-metaworld"

device: "cuda"

benchmark_name: "metaworld"
sub_benchmark_name: "ML45"
